import type { Metadata } from "next";
import "./globals.css";



export const metadata: Metadata = {
  title: "shop",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`antialiased`}
      >
        <div className="flex items-center justify-between text-white bg-red-500 rounded-xl mx-auto p-3 mt-5 w-[80%]">
           <div >shop</div>
           <div className="flex item-center gap-x-5">
              <p>sign in</p>
              <p>cart</p>
           </div>
        </div>
        {children}
      </body>
    </html>
  );
}
